import { BMIboxesDirective } from './bmiboxes.directive';

describe('BMIboxesDirective', () => {
  it('should create an instance', () => {
    const directive = new BMIboxesDirective();
    expect(directive).toBeTruthy();
  });
});
