import { DoctordatefilterPipe } from './doctordatefilter.pipe';

describe('DoctordatefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DoctordatefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
